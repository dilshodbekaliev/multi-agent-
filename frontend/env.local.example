# Copy this file to .env.local and adjust if needed.
# NEVER commit the real .env.local file.

# Base URL of the backend API.
# Defaults to http://localhost:8000 if unset, so local dev works with no config.
# Set this to your deployed backend URL (e.g. Render) for production builds.
NEXT_PUBLIC_API_URL=http://localhost:8000
